[project]
name = "osu-most-played-downloader"
version = "1.0.0"
description = "Download a user's most played osu! beatmaps"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "python-dotenv",
]